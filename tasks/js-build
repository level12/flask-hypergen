#!/usr/bin/env bash
# [MISE] description="Build the packaged Hypergen browser runtime"
set -euo pipefail

repo_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

exec aube run build
